//! API for accessing the content of an AGS file.

use std::path::Path as stdPath;

/// Access grant store based on AGS text file.
pub struct AGSFile {}

impl AGSFile {
    pub fn load(file: &stdPath) -> Self {
        todo!();
    }
}
